#include <stdio.h>
/**
 * add - a function that adds two integers
 * and returns the result.
 * @a: input variable
 * @b: input variable
 *
 * Return: the result (c)
 */

int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
