#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: input variable
 * @b: input variable
 * Return: 0 success
 */

int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
